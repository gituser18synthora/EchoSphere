"""MCP server exposing tenant-safe knowledge tools for agent interoperability.

Shares the exact same KnowledgeService as REST and the voice runtime — MCP is
an interface, not a second implementation.
"""
